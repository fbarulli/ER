exclude = ["models", "data", "results", "embeddings", "logs"]

[lint.per-file-ignores]
"DATA_PIPE/**" = ["N999"]  # owner-named packages (uppercase by directive)
"TRAIN/**" = ["N999", "BLE001"]
"lib/training.py" = ["BLE001"]

