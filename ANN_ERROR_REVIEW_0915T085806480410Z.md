# ANN error review — gated run 0915T085806480410Z

## Scope

This review uses the gate artifact retrieved from Colab:

`colab_retrieved/0915T085806480410Z/gate_results.csv`

The runtime was torn down at training step 102/700 (epoch 1.46/10). The
post-training 3,000-SKU inference predictions and labeled ANN report were not
created, so recall, precision, adjusted Rand, over-merge, and under-merge
cannot be recomputed from this artifact alone.

## Gate outcome

| Outcome | Rows |
|---|---:|
| hard_no | 92,985 |
| proceed | 29,019 |
| fallback | 13,765 |
| Total | 135,769 |

Similarity ranged from 0.1111 to 1.0000 (mean 0.5626).

The dominant reasons were:

| Gate reason | Rows |
|---|---:|
| No volume overlap | 73,175 |
| Volume, pack, flavor all compatible | 29,019 |
| Low raw volume confidence | 12,724 |
| No pack overlap | 8,194 |
| Low raw pack confidence | 982 |

## Package-type safety check

The new gate emitted 326 explicit `Package type mismatch` decisions. All 326
were `hard_no`; none were `proceed` or `fallback`. Their similarity scores
ranged from 0.1250 to 1.0000 (mean 0.6032), confirming that similarity alone
would not be a safe contradiction check.

## Pending completion checks

After a complete gated run, this review must be rerun with the model's labeled
3,000-SKU predictions and report directory to measure:

- ANN candidate recall at @1/@5/@10;
- pair precision/recall and adjusted Rand;
- over-merge and under-merge rates;
- attribute error rates, including `package_type_conflict`;
- whether the package-type veto removes false merges without reducing true-pair
  recall.

